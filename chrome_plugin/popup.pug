doctype html
html
	head
		style.
			body{
				min-width: 120px;
			}
			button,label{
				width: 100%;
				user-select: none;
				margin-bottom: 8px;
			}
	body
		button(data-action="closeDone") Close done
		
		label
			input#useFFmpeg(type="checkbox")
			| Use FFMpeg instead of 9xBuddy for VOE
		
		br
		label
			input#useSeriesFolder(type="checkbox")
			| Put Series in folders for FFMpeg downloads
		
		br
		label
			input#maxFfmpeg(type="number")
			| Max FFMpeg instances that run at the same time
		
		script(src="js/popup.js")